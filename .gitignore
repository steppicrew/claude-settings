# Ignore everything by default
*

# Track these top-level files
!.gitignore
!README.md
!CLAUDE.md
!sync.sh
!install-alias.sh
!claude-memory-init.sh

# Track config dir (symlinks only, not contents — contents live in private repos)
!config/
config/*
!config/current
